## アレンジしてオリジナル問題を作成
あなたは魔法世界の道具屋の店主です。店では「通常アイテム」と「魔導書」を販売しています。
本日限定のキャンペーンとして、以下の 2つの割引ルール を適用することにしました。まとめ買い割引: 合計金額が 1,000ゴールド以上 の場合、100ゴールド引き になる。
魔導書ボーナス: さらに、買った商品の中に「魔導書」が含まれている場合、まとめ買い割引後の金額から 10% OFF（端数切り捨て）になる。
商品の値段 $P$ ゴールドと、その商品が魔導書かどうかを表すフラグ $T$（1 なら魔導書、0 なら通常アイテム）を入力として受け取り、
最終的な会計金額を計算するプログラムを作成してください。

## 入力例１
800 0
150 0

### 解答コード
import math

def calculate_checkout():
    # 1. 入力の取得（2行分）
    p1, t1 = map(int, input().split())
    p2, t2 = map(int, input().split())
    
    # 小計の計算
    total = p1 + p2
    
    # 条件1: 1000ゴールド以上のまとめ買い割引
    if total >= 1000:
        total -= 100
        
    # 条件2: 魔導書が含まれているか判定 (t1 または t2 が 1 なら True)
    if t1 == 1 or t2 == 1:
        # 10% OFF（切り捨て処理）
        total = math.floor(total * 0.9)
        
    print(total)

if __name__ == '__main__':
    calculate_checkout()
