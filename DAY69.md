# アレンジをしてオリジナル問題を作成
あなたは時空間を越える旅客輸送機「クロノス号」の運行 AI です。
クロノス号は各タイムポート（停留所）で乗客（時空旅人）の乗降を行いながら航行しています。初期状態（タイムポート 0 到着前）において、機内の乗客数は 0 人 です。
タイムポート 1 では、新たな乗客が $x$ 人 搭乗しました。タイムポート 2 では、$y$ 人 の乗客が過去または未来へと降機し、続いて $z$ 人 の新たな乗客が搭乗しました。
現在のタイムポート 2 出発時点における、クロノス号の最終的な機内総乗客数を算出するアルゴリズムを構築してください。

## 入力例
10
3
4
## 出力例
11

### 解答例
import sys

def main():
    # 標準入力から全行を読み込む
    lines = sys.stdin.read().splitlines()
    if not lines:
        return
    
    x = int(lines[0])
    y = int(lines[1])
    z = int(lines[2])
    
    # 最終的な乗客数の計算: x - y + z
    current_passengers = x - y + z
    
    print(current_passengers)

if __name__ == "__main__":
    main()
