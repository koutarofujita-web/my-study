# 🛡️ Pseudo Anti-Virus v2.0 (CLI Security Scanner)

Linux / macOS のターミナル環境で動作する、シンプルな Bash 製の疑似アンチウイルス（セキュリティスキャナー）です。  
パーミッション設定、ファイル属性、シェルの検索コマンド（`find`, `grep`）の仕組みや、セキュリティツールの基本的な検知・隔離・削除・ログ出力ロジックを学習するために開発しました。

---

## 🚀 主な機能

1. **危険なパーミッションの検知**
   - 全開放されている危険な権限（`777`）を持つファイルを自動検出。
2. **隠しファイルの検知**
   - システム内に不審に潜むドットファイル（`.filename`）を検出。
3. **危険なコマンド記述の検知（シグネチャ分析の模倣）**
   - スクリプト内に含まれる破壊的コマンド（`rm -rf /` 等）をテキスト検索して特定。
4. **擬態ファイル（ダブル拡張子）の検知**
   - `invoice.pdf.sh` のように、ユーザーを騙す目的でファイル種別を偽装したスクリプトを検出。
5. **ハッシュ値判定（MD5ブラックリスト照合）**
   - ファイル名変更による偽装に対抗するため、`md5sum` を用いてデータのハッシュ値を計算・照合。
6. **インタラクティブな対処（隔離 / 完全削除）**
   - 脅威検出時、ユーザーの選択に応じて `quarantine/` ディレクトリへの無害化移動（隔離）または完全削除を実行。
7. **監査ログ（Audit Log）の自動生成**
   - 実行日時、スキャン結果、対処内容をタイムスタンプ付きで `scan.log` へ記録。

---

## 📂 使い方

### 1. 準備（実行権限の付与）
```bash
chmod +x pseudo_av.sh


