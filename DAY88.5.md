# アレンジしてオリジナル問題を作成
あなたは時間経過によってアクセス鍵のハッシュ値が変化する特殊な分散ログシステムを構築しています。
各ログブロックは、前後のブロックとの関連性（グラフ構造）
と暗号化ハッシュ、およびアクセス時点のタイムスタンプに基づく動的なキー計算によって保護されます。
## 課題定義
以下の条件を満たすバックアップデータの検証および復元関数 verify_and_decrypt を実装してください。
データ構造入力として、以下のデータ形式（JSONオブジェクト相当）が与えられます。
nodes: ノード ID（整数）をキー、ノード情報
（暗号化ペイロード、依存先ノード ID リスト、タイムスタンプ $t$）を値とする辞書。
root_id: 検証を開始するルートノード ID。鍵の動的生成規則 (Dynamic Key Generation)
ノード $i$ の復号キー $K_i$ は次のように計算されます。$$K_i = \text{SHA256}(t_i \parallel P_i)
$$$t_i$: ノード $i$ のタイムスタンプ（Unix Epoch秒 / 整数）$P_i$: ノード $i$ 
が依存するすべてのノードの復号済みペイロード（文字列）を、依存先ノード ID の昇順でソートして
連結した文字列（依存ノードがない場合は空文字列 ""）。ノードの復号処理ペイロードは $K_i$ 
（の先頭16バイトなど）を用いた XOR 暗号化（または AES-128-CBC 相当）が施されています。
簡略化仕様（アルゴリズム重視の場合）: 暗号化データは $K_i$ の 16 進数文字列表現と XOR された
文字列として与えられます。非巡回・整合性チェック (DAG & State Validation)ノード間の依存関係は
有向グラフを構成します。依存関係に循環（サイクル）が存在する場合、あるいは依存先ノードが存在しない場合は
即座にエラー（InvalidDependencyError）を発生させてください。ルートノードから到達不能な孤立ノードは無視し
、ルートノードから参照される全ノードを正しいトポロジカル順序で復号してください。

### 入力例
data = {
    "root_id": 1,
    "nodes": {
        1: {
            "timestamp": 1700000030,
            "dependencies": [2, 3],
            "encrypted_data": "...", # K_1 で暗号化されたデータ
        },
        2: {
            "timestamp": 1700000010,
            "dependencies": [],
            "encrypted_data": "...", # K_2 で暗号化されたデータ
        },
        3: {
            "timestamp": 1700000020,
            "dependencies": [2],
            "encrypted_data": "...", # K_3 で暗号化されたデータ
        }
    }
}
