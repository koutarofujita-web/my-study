## やったこと

- `ssh-keygen` コマンドを使用し、`ed25519` 方式でSSH鍵ペア（秘密鍵・公開鍵）を作成した。
- Linuxのファイル権限（パーミッション）について学習した。

## 実験と気付き

- 秘密鍵の権限を `chmod 777` に変更し、接続時にエラーが出ることを確認した。
- Linuxシステムが「危険な鍵」と判断し、鍵認証を拒否する仕組みを体験した。
- `chmod 600` に戻すことで、安全な状態で接続できることを確認した。
