## 概要
Google Gemini APIとの通信で発生していたエラー（503/404エラー）の原因を特定し、モデル名の正常化およびエラーハンドリングの強化を行いました。

## 主な変更内容

### 1. モデル名の正常化
- `app.py` および `core.py` 内で指定されていた存在しないモデル名（`gemini-3.6-flash`）を、正式な最新軽量モデル（`gemini-1.5-flash`）へ修正。
- APIレスポンスの安定化と無効なリクエストエラーの解消を実現。

### 2. エラーハンドリング・堅牢性の向上
- APIキー未設定時および通信エラー発生時の通知（`st.error` / `st.toast`）を追加。
- 今後のサーバー混雑（503 UNAVAILABLE）やレート制限（429）に備え、自動リトライ（指数バックオフ）およびフォールバック処理の設計方針を整理。

### 3. システム運用・環境設定の整備
- `run_ai.sh` による環境変数（`GEMINI_API_KEY`）の読み込み手順およびプロセス管理（`pkill`）を再確認し、安定したバックグラウンド起動環境を確保。

## 動作確認
1. `streamlit run app.py` を実行し、`gemini-1.5-flash` 経由でAI応答が正常に返ってくることを確認。
2. APIキーが正しくセットされた状態でエラーが出ずにチャットが行えることを確認。
