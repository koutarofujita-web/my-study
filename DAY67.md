# 🛡️ Pseudo Anti-Virus (CLI Security Scanner)

Linux / macOS のターミナル環境で動作する、シンプルな Bash 製「疑似アンチウイルス（セキュリティスキャナー）」です。  
パーミッション設定、ファイル属性、シェルの検索コマンド（`find`, `grep`）の仕組みや、セキュリティツールの基本的な検知・隔離ロジックを学ぶために作成しました。

---

## 🚀 主な機能

1. **危険なパーミッションの検知**
   - 全開放されている危険な権限（`777`）を持つファイルを検出。
2. **隠しファイルの検知**
   - システム内に不審に潜むドットファイル（`.filename`）を検出。
3. **危険なコマンド記述の検知（シグネチャ分析の模倣）**
   - スクリプト内に含まれる破壊的コマンド（`rm -rf /` 等）をテキスト検索して特定。
4. **擬態ファイル（ダブル拡張子）の検知**
   - `invoice.pdf.sh` のように、ユーザーを騙す目的でファイル種別を偽装したスクリプトを検出。
5. **インタラクティブな隔離機能 (Quarantine)**
   - 脅威を検知した際、ユーザーの確認（`y/n`）を経て、危険なファイルを安全な `quarantine/` ディレクトリへ自動移動（無害化）。

---

## 📂 使い方

### 1. リポジトリのクローン・準備
```bash
chmod +x pseudo_av.sh
