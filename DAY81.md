# Fake Alert App (Python Tkinter)

Pythonの `tkinter` ライブラリを使用して作成した、セキュリティ警告風のGUIデスクトップアプリケーションです。  
非同期処理による動的なUI更新、実システム情報の動的取得、安全装置（キルスイッチ）の実装など、PythonにおけるGUIプログラミングおよびイベントハンドリングの学習目的で開発しました。

※本プログラムは自身の所有する環境での学習・検証目的のみで制作されたものであり、実害のある処理（ファイルの暗号化や破壊など）は一切含まれていません。

---

## 主な機能・演出

* **動的なシステム情報の表示:** `getpass` および `platform` モジュールを使用し、実行環境のユーザー名・ホスト名・OS情報をリアルタイムに取得して画面へ反映。
* **対話型ミニゲーム（解除機能）:** ランダム生成される4桁の解除コードを時間内に入力することで制御を取り戻すインタラクティブ要素。
* **画面点滅 & 効果音（ビープ音）:** `after` メソッドを利用した非同期処理により、背景色の点滅と音声を非閉塞で制御。
* **カスケードウィンドウ生成:** タイマー進行に合わせて小さな警告サブウィンドウ（`Toplevel`）がランダムな位置に自動出現。
* **リアルタイムログ流し込み:** 擬似的なIPアドレスやポート走査ログを高速でテキストボックスへ連続描画。
* **二重キルスイッチ（安全設計）:** `Esc` キーまたは `Ctrl + Shift + Q` により、生成されたサブウィンドウを含めて一括でプロセスを安全破棄・終了する設計。

---

## 技術的なポイント・学び

1. **非同期UI制御 (`after` メソッド):**  
   メインスレッドをブロック（フリーズ）させることなく、タイマーカウントダウン、背景色変更、ログ追加などの定期処理を並行して実行する設計手法を習得しました。
2. **イベントハンドリングとキーバインド:**  
   `bind` メソッドを用いた特定のショートカットキー検知や、ウィンドウ閉じるボタン（`WM_DELETE_WINDOW`）の割り込み制御により、意図しない挙動を防ぐ安全対策を実装しました。
3. **リソース管理と安全な破棄:**  
   生成された親ウィンドウおよび子ウィンドウ（`Toplevel`）のインスタンスを配列で管理し、キルスイッチ発動時に漏れなく一括破棄（`destroy()`）する構造を構築しました。

---

## 動作環境

* **OS:** Linux (Debian / Ubuntu / Raspberry Pi OS 等) または WSL
* **Python:** 3.x
* **依存パッケージ:**
  * `python3-tk` (Tkinter GUIライブラリ)
  * `alsa-utils` (音声再生用コマンド `aplay` 依存)

---
