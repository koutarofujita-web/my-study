# アレンジしてオリジナル問題を作成
名探偵のあなたは、怪盗から送られてきた謎の「予告状（スペース区切りの文字列）」を手に入れました。
この予告状には、どうやら無駄に長い単語と、適当に添えられた短い単語が混ざっているようです。

あなたは予告状の**「一番無駄に長い単語の長さ」**と**「一番やる気のない短い単語の長さ」**のギャップ（差）を測ることで、怪盗のメンタルの揺らぎを数値化することにしました。

文字列が入力されるので、**最も長い単語の文字数**と**最も短い単語の文字数**の「差」を出力するプログラムを作成してください。

## 入力例
4
monster cat a hyperventilation

## 出力例
17

### 解答例
def main():
    # 1行目：単語の数 N（Pythonの処理では直接使用せず解法可能）
    n = int(input())

    # 2行目：スペース区切りの単語をリストとして取得
    words = input().split()

    # 各単語の長さを取得
    word_lengths = [len(w) for w in words]

    # (最大文字数) - (最小文字数) を計算して出力
    gap = max(word_lengths) - min(word_lengths)
    print(gap)

if __name__ == "__main__":
    main()
