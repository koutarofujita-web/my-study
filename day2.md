##day2

- ファイアウォール（UFW）の起動確認とWebポート（80/tcp）の開放
- sudo journalctl -f` を使った、リアルタイムでのシステムログ監視
- ls -la` コマンドによる、隠しファイル（ドットファイル）の検出
- top` コマンドによる、実行中プロセス（PID）の監視
- sudo ss -antp` コマンドによる、通信待ち受け状態（LISTEN）の調査と。

##学んだ事

- 裏で動く守護神（`sshd`）の特定
