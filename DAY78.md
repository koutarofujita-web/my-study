## 概要
`core.py` 実行時に発生していた Gemini API の `404 NOT_FOUND` エラーを修復し、AIアシスタント（ニューロンくん）による診断アドバイス出力を復旧しました。

---

## 発生していた問題
* 旧指定モデル（`gemini-2.0-flash` / `gemini-2.5-flash`）が API 側で提供終了・非推奨となったため、実行時に `404 NOT_FOUND` で停止する状態が発生。

---

## 実施した対応
1. `google-genai` SDK の利用可能モデル一覧（`client.models.list()`）を確認。
2. 動作確認済みの最新安定モデル **`gemini-3.6-flash`** にコード内の指定を変更。
3. `python3 core.py` による診断結果出力の疎通テストをクリア。

---

## 動作確認結果
* ノード接続 (`security`, `system`, `log`) およびリスクスコア算出の成功を確認。
* AIアシスタントによるレスポンスの生成・表示を確認。
